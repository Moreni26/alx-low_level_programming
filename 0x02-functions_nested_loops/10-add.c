#include "main.h"
/**
 * add -A function adds two integers and returns the result
 * @a: This is  an integer
 * @b: This is also an integer
 * Return: sum of integer added
 */
int add(int a, int b)
{
	return (a + b);
}
